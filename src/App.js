import React from 'react';

import { FormContainer, ListContainer } from 'containers';

const App = () => (
  <div className="app">
    <h1>todo list</h1>
    <FormContainer />
    <ListContainer />
  </div>);

export default App;
